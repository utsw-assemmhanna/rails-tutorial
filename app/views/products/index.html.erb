<h1>Products</h1>

<h2>
  <%= link_to "New Product", new_product_path%>
</h2>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
 <% end %>
</div>
